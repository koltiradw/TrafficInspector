name: 'CodeQL config for frontend'
tools: stable
languages: javascript
paths:
  - assets
  - build-scripts
  - build-templates
  - src
  - static
paths-ignore:
  - 'node_modules'
  - '**/node_modules'
queries:
  - uses: security-extended
  - uses: security-and-quality
